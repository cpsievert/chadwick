% Generated by roxygen2 (4.1.1): do not edit by hand
% Please edit documentation in R/cwevent.R
\name{cwevent}
\alias{cwevent}
\title{Extract detailed information about individual events.}
\usage{
cwevent(year)
}
\arguments{
\item{year}{The year to obtain event information}
}
\description{
Extract detailed information about individual events.
}
\examples{
# acquire event data from 1960
df <- cwevent(1960)

# tack on batter's name
data(players, package = "chadwick")
library(dplyr)
p <- players \%>\%
  rename(BAT_ID = Player.ID, BAT_TEAM_ID = Team) \%>\%
  mutate(Batter.Name = paste(First.Name, Last.Name)) \%>\%
  select(Year, BAT_TEAM_ID, BAT_ID, Batter.Name)
df2 <- left_join(df, p)

# who had the most strikeouts in 1960?
df2 \%>\%
  filter(EVENT_DES == "Strikeout") \%>\%
  count(Batter.Name) \%>\%
  arrange(desc(n))
}
\references{
\url{http://chadwick.sourceforge.net/doc/cwevent.html}
}

